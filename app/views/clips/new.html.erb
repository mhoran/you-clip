<div class="container">
  <h1>YouClip!</h1>

  <%= simple_form_for @clip do |form| %>
    <div class="form-group">
      <%= form.input :url, label: 'YouTube URL' %>
    </div>
    <%= form.submit class: 'btn btn-default' %>
  <% end %>
</div>
